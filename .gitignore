node_modules/
dist/
*.log

# Design record kept local during the private phase (owner directive: land docs,
# push code only). Remove these two lines to publish the glossary + ADRs when the
# repo goes public.
/CONTEXT.md
/docs/
